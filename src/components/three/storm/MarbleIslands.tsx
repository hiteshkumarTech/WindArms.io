/**
 * Superseded in the cinematic art pass by:
 *   - SkyCitadel.tsx     — the focal Wind Temple + Aeon Ring
 *   - SkyArchipelago.tsx — satellites, bridges, distant skyline, airships
 * Kept as a shim so stale imports fail loudly at review rather than
 * silently rendering the old sparse world.
 */
export { default } from './SkyCitadel';
