/** Props every registered landing section receives from the view. */
export interface SectionRenderProps {
  /** True once the boot preloader has handed off (hero gates on this). */
  bootDone: boolean;
}
