# WindArms — TODO

Project-level TODOs, not code TODOs (there are no `// TODO` comments in the codebase as of 2026-07-12 — see [known-bugs.md](known-bugs.md)). Every item below is pulled from the existing v1 backlog ([roadmap.md](roadmap.md)) and the v2 scope ([versions/v2.md](versions/v2.md)) — priority tiers are an organizing judgment call, not sourced from prior prioritization, so re-rank freely as real constraints (deadlines, user feedback) emerge.

## Priority

### HIGH

- **Kael FP-arm two-bone IK: pose calibration DONE (0.00cm both hands, re-confirmed 2026-07-25), materials DONE (PBR gloves/sleeve/armor), and procedural reload/inspect presentation DONE (Step 7C, 2026-07-25).** Structural blockers resolved and reach deficit fixed in Steps 6E–6G; Step 6H/6I applied full human visual calibration: right hand-basis +30° X, left hand-basis `[30, 30, -10]`, left shoulder assist `[0.06, -0.009, -0.05]`, both wrist-rotation-weights 1.0, left finger curl 0.30 (right finger curl still 0). Real-browser motion test confirmed no hand detachment/wrist snapping/exploded triangles/scope obstruction. Step 7A shipped baked PBR materials (dark charcoal gloves, pale sleeve trim, titanium/gold armor accents, restrained cyan seam). Step 7C (2026-07-25) added procedural (not authored-clip) reload/inspect presentation: the left hand visibly releases toward a temporary weapon-relative action target and returns exactly to the calibrated grip pose; the right hand and all IK/grip/weapon values are completely untouched — see [decisions.md](decisions.md) for the architecture and [changelog.md](changelog.md) for the full real-browser validation record (5x/5x stress test with zero drift, interruption matrix, `/v2/play` pause-freeze proven byte-identical across a real 1-second gap). **What's left**: right-side finger curl (never reached its calibration stage), human visual sign-off on materials AND the new reload/inspect presentation (nothing committed), and human visual calibration of the two new action-target anchors (`RELOAD_LEFT_HAND_LOCAL`/`INSPECT_LEFT_HAND_LOCAL` in `lib/v2/weapons/actionTargets.ts` — currently reasoned conservative estimates, not checked against the running scene the way the original grip anchors were; a `?anim=1` "show action target" marker exists for exactly this). Full detail: [design/weapons/vortex-rifle.md](design/weapons/vortex-rifle.md) §22b–§22c, [forge/operator-pipeline.md](forge/operator-pipeline.md) §5, [decisions.md](decisions.md). Runtime body derivatives, the FP-arms extraction, and the grip-anchor target system that this IK layer builds on are all already shipped — see [forge/kael-v0.1-inspection.md](forge/kael-v0.1-inspection.md) and §22a.
- **Milestone 8 — Kael first-person full-body awareness: Steps 8A/8B/8B.1/8C/8C.1/8D(bones)/8E-B DONE and COMMITTED (`a5967fe`); Step 8E-A DONE; Step 8E-C/8E-C.1/8E-C.2 superseded; Step 8E-C.3/8E-C.3.1/8E-C.3.2 COMMITTED (`128732f`); Step 8E-D (shadow visual/light calibration) COMMITTED (`df632ca`); Step 8E-D.1A (measurement) DONE; Step 8E-D.1 (player-centered texel-stabilized frustum) COMMITTED (`3d06941`); Step 8E-E audit DONE (READY WITH PREREQUISITES); Step 8E-E.0 (live-motion shimmer prerequisite) DONE (PASS WITH WARNING); Step 8E-E (Option 1 — range-only production caster-ownership switch) TECHNICALLY VALIDATED, not yet committed.** Steps 8E-C/8E-C.1/8E-C.2 progressively fixed the reach architecture down to 0.00cm error/assist everywhere. Steps 8E-C.3/8E-C.3.1 built and isolated the external dev-only review camera + per-action weapon presentation; 8E-C.3.2 fixed a camera-preset front/rear/left/right naming bug. All three committed and pushed 2026-07-30 (`269785b`, `2badd5d` the quality-protocol skill, `128732f`). Full near-miss/root-cause traces in [decisions.md](decisions.md). **Step 8E-D (2026-07-31, committed `df632ca`) — dev-only shadow visual/light calibration, per the repository's own 4-step sequence (8E-D = "visual/light calibration," distinct from 8E-E's "lifecycle/performance/quality gating and final caster-ownership switch" — this step does NOT touch production shadow ownership).** Added two receiver modes (`production` matches the real floor's `STORM.abyss` token, `readable` keeps the original mid-gray) — found the real floor is dark enough that a cast shadow reads with genuinely low contrast against it in production mode, a real pre-existing characteristic now visible for the first time. Tested shadow-bias/normalBias across a 10x range (0 to -0.008) and shadow-map resolution (1024² vs 2048²): both showed NO visible difference — an honest negative result, not a papered-over guess (the test methodology was independently confirmed sound via the clearly-visible receiver-mode comparison). Root cause for the resolution finding: `RANGE_SHADOW_CAMERA_BOUNDS`' frustum spans the entire floor, so texel count isn't the bottleneck. Self-shadowing (weapon-on-arm) tested the same evidence-gated way, also no visible effect, left disabled. **Step 8E-D.1A (measurement-only) then measured the real deformed-mesh light-space footprint across 13 action states (0.759m worst width / 1.356m worst height / 3.755m worst depth span), approving a 3.5m×6m rectangular candidate with continuous no-dead-zone texel snapping over a square or a dead-zoned alternative — see [decisions.md](decisions.md).** **Step 8E-D.1 (2026-07-31) implements tracking against that candidate**: a pure snapping module, a fixed light-space basis (a real `Object3D`-vs-`Camera` `lookAt()` convention bug was caught by its own round-trip unit test before shipping — see [decisions.md](decisions.md)), an imperative controller that tracks the player's ground anchor at a fixed Y (never airborne Y), and a `frustumMode` toggle (static/player-centered, defaulting to player-centered) with full A/B panel readouts. 30 new tests (479/479 total), `typecheck`/`lint`/`build` clean, GLB hashes unchanged, zero console errors across an extensive real-browser validation (A/B matrix, 9-state coverage sweep, movement-tracking coverage, review-camera isolation, static-mode regression, route-remount cleanup). Temporary performance instrumentation added, measured (draw calls/triangles/textures identical across all three configs — resolution/frustum size don't add scene draw overhead; absolute frame-time numbers were environment-bound headless/software-rendering noise, not reported as a real-hardware claim), then fully removed. **Shadow-sharpness visual verdict deliberately left to human review** — texel density improves ~37x/~21x per axis by construction, but Claude does not claim the final "materially sharper" visual judgment. Human-approved and committed 2026-08-01 (`3d06941`). **Step 8E-E audit (2026-08-01, read-only) classified the production caster switch READY WITH PREREQUISITES** — architecture sound, but required a live-motion shimmer numerical gate before implementation. **Step 8E-E.0 (2026-08-01) ran that gate**: zero unexplained texel-snap ping-pong across 11 real-browser motion phases (including two deceleration-tail sweeps that came within 0.09-0.13mm of an exact texel boundary), quantization error stayed within budget, no cumulative drift, mouse-look/review-camera provably cannot move the frustum — **PASS WITH WARNING** (no hysteresis needed; a small bounded non-accumulating light-Y coupling and this environment's low headless frame rate were disclosed as informational, not blocking). **Step 8E-E (2026-08-01) then implemented the approved Option 1** — one typed `RANGE_SHADOW_CASTER_POLICY` (`'fp-arms' | 'full-body'`, canonical `'full-body'`, one-line rollback), a pure exhaustively-tested exclusivity decision function, `KaelFirstPersonArms.tsx` extended with an opt-in `casterPolicy` prop (default-safe, so `/v2/play` is unaffected by construction, not just by convention), and a real cross-session leakage risk (a stale dev-review `frustumMode` value leaking into a flag-free production session) found and fixed BEFORE browser testing via a new `allowDevModeOverride` prop. 494/494 tests (15 new), `typecheck`/`lint`/`build` clean, GLB hashes unchanged, `/v2/play` and V1 `/play` untouched (now enforced by a source-text regression test, not just by omission). Full real-browser validation: flag-free production shows 0 debug panels + FP arms fully visible + 0 console errors across the full state matrix; review route still shares exactly 1 clone; 5x route remounts clean; rollback verified both directions. Performance: +2 draws/+72,828 triangles (full-body clone is higher-poly, as expected) — clean deterministic facts; frame-time deltas fell in the informational "warning" band of this specific headless sandbox, not reported as a real-hardware claim. **Range-only** — `/v2/play` deliberately deferred pending its own death/respawn/restart validation. **Not yet committed — awaiting human visual approval.** Full detail: [decisions.md](decisions.md), [changelog.md](changelog.md).
- ~~**Found 2026-07-25, not fixed: `VortexFireSystem.tsx` never clears `triggerQueued.current` on the "blocked because reloading" exit path**~~ — **FIXED 2026-07-26 (Step 7E, commit `03f9ffb`).** Fire-gating decision extracted into a pure `resolveTriggerGate` (`lib/v2/weapons/triggerIntent.ts`); the reload-blocked exit now clears the queued flag like the other two exits already did. 9 new deterministic tests. See [decisions.md](decisions.md)/[changelog.md](changelog.md).
- **Deferred from Step 7A (2026-07-24), explicitly out of scope until a later milestone: full-body awareness, an operator showcase, match intro, victory presentation, and third-person spectator systems.** First-person combat is the only gameplay camera the Kael arms/materials/animation work has needed to serve so far — none of the above exist yet in any form and were deliberately not started.

- ~~**The Vortex Rifle GLB needs to be re-exported as a single assembled mesh**~~ — **RESOLVED 2026-07-17.** The v0.2 source (`Hitem3d-1784224974921.glb`) is a clean single mesh; the runtime derivative (`tools/make-vortex-runtime.mjs`) now ships `public/v2-art/vortex-rifle.glb` (LOD0, 139,598 tris, 0.84 MB) and `vortex-rifle.lod1.glb` (LOD1, 55,834 tris, 0.57 MB). Real mesh confirmed rendering: landing hero → LOD0, `/v2/range` + `/v2/play` first-person → LOD1 (via `requestedLod={1}`). The old broken multipart v0.1 preview stays archived. **Remaining, lower-priority follow-ups** (see `docs/forge/vortex-rifle-v0.2.md`): the derivative is automatic decimation, not professional retopology (Blender bake pass = the path to v1.0); single `pbr_material` has no accent/energy/tint-named slot, so skin/accent tinting is inert until a material split; no normal map, so fine detail is lost at the current ratio.
- **Skyfront Trial (`/v2/play`) follow-ups** (2026-07-17, updated 2026-07-18, [gameplay/skyfront-trial.md](gameplay/skyfront-trial.md)): the playable loop, match state machine, weapon, LOD, FP pose, muzzle/tracer origin and a Low/Medium/Max difficulty system are all real now. The arena is still a **procedural blockout** and the drone a **temporary training target** — both need production art/replacement. Also open: real playtesting of the Low/Max multipliers (`difficulty.ts` — currently first-pass numbers, not soak-tested; Medium is the only playtested tier), Wind Lift arc tuning, **first-person operator hands + real hand/grip contact** (the 2026-07-18 pose fix corrects orientation but is still a *floating* viewmodel, not a *held* one — waiting on the Phase 6 operator GLB + hand IK, the operator architecture from the prior milestone is ready), a real Blender-exported v1.0 muzzle socket (the current `vortexRuntimeAnchors.ts` anchor is a temporary hand-measured stand-in — delete it once `socket_muzzle` exists), real reload/inspect animations, and touch controls (deliberately deferred — mobile shows a desktop-recommended notice). Gameplay audio (fire/reload/impact/spin-down) is 100% procedural Web Audio synthesis, per `design/weapons/vortex-rifle.md` §15 and `design/audio.md`'s "reuse v1's approach" direction — confirmed as the settled architecture (not a placeholder) in Step 7F, 2026-07-26; the real-audio-file probe that was causing 404s (no manifest/files ever existed) was removed rather than satisfied with placeholder files. Step 7G (2026-07-26) built the signature layered shot/turbine/reload sound identity on top of that architecture — **technically complete (architecture, lifecycle, offline peak/RMS measurement all verified) but awaiting a human listening pass via `/v2/range?audio=1` before being called creatively final**; content beyond the four-layer shot/turbine (the fuller "turbine whine / chuff / EM-crack / wind resonance" identity from `design/weapons/vortex-rifle.md` §15) remains open future work. See [decisions.md](decisions.md)/[changelog.md](changelog.md).
- Hero ability system for Kael Aurin and Veyra Solace ([gameplay/abilities.md](gameplay/abilities.md), [gameplay/operators.md](gameplay/operators.md)) — bios/roles/signature weapons exist, but passive/signature/ultimate abilities are undefined anywhere in code. This is the headline v2 feature.
- **Reconcile the 2026-07-14 production backlog against existing code-confirmed canon** before more blueprints are built on top of it: backlog names Vortex Rifle / Stormbreaker SMG / Aeon Sniper / Tempest Shotgun / Cyclone LMG / Zephyr Pistol (weapons) and Kai / Lira / Zephyr / Orion (operators) — none of which match `shared/windWeapons.ts` (Aeolus Rifle, Vortex Carbine, Tempest Cannon, Gust Blade) or `operators.ts` (Kael Aurin, Veyra Solace). "Zephyr" also collides within the backlog itself (operator name and weapon name). Not resolved — raise with the user rather than guessing which roster wins.
- v1's Vortex SMG vs. v2's Vortex Rifle: **partially resolved 2026-07-16** ([decisions.md](decisions.md)) — v2's `vortex` is now "Vortex Rifle" (was "Vortex Carbine"), different id namespace than v1's `smg`, no runtime collision. Still open: the marketing name "Vortex" is reused for two different weapons across v1/v2 — decide before both are ever shown together (e.g. a cross-version weapons page) — see [gameplay/weapons.md](gameplay/weapons.md#v2-arsenal-windweaponsts).
- `docs/gameplay/operators.md` still describes Veyra Solace's signature weapon as "Vortex Carbine" — stale since the 2026-07-16 rename, not yet fixed.
- v2 new UI ([design/ui.md](design/ui.md), [design/art-direction.md](design/art-direction.md)) — note the V2 preview site already has its own marketing UI system (STORM tokens, section shells); clarify its relationship to a future in-game UI.
- v2 matchmaking rework (scope not yet detailed beyond v1's fill-based system — needs a design pass)
- Answer the open questions in [design/skyfront.md](design/skyfront.md): is the Skyfront one contiguous space or per-POI arenas; how many POIs are planned; how does it relate to v1's four existing maps (or the backlog's Celestial City / Frosthaven / Verdant Ruins / Aeolus Station, which also don't match either existing map set).

### Production backlog (2026-07-14, from the user)

**Names only — style is NOT from this backlog's source image.** Confirmed 2026-07-14 ([decisions.md](decisions.md)): "War Above The Storm" ([design/art-bible.md](design/art-bible.md), `image-1.png`, the `STORM` tokens) stays canonical for materials/colors/silhouette/everything visual. This list's source, `docs/images/ChatGPT Image Jul 13, 2026, 11_04_24 PM.png`, is a names/composition reference only.

Also note: the user's typed list doesn't exactly match that image's own labels for 3 of the 6 weapons — image says **Inferno Shotgun** (typed as "Tempest Shotgun"), **Windlance Marksman** (typed as "Cyclone LMG"), **Nova Launcher** (typed as "Zephyr Pistol"). Both variants are recorded below; confirm which is correct before more blueprints use the wrong name.

- Weapons: Vortex Rifle (blueprint in progress — [design/weapons/vortex-rifle.md](design/weapons/vortex-rifle.md)), Stormbreaker SMG, Aeon Sniper, **Tempest Shotgun / Inferno Shotgun (name conflict)**, **Cyclone LMG / Windlance Marksman (name conflict)**, **Zephyr Pistol / Nova Launcher (name conflict)**
- Operators: Kai, Lira, Zephyr, Orion (matches the image exactly)
- Maps: Celestial City, Frosthaven (image: "Frosthaven Base"), Verdant Ruins, Aeolus Station
- Vehicles: Wind Bike, Sky Carrier, Cargo Airship
- UI: Main Menu, HUD, Inventory, Matchmaking, Scoreboard

### MEDIUM

- Ranked matchmaking, ELO, and seasons (v1 backlog)
- Enabling `LAG_COMP` by default in v1 — currently flagged off pending a soak test ([technical/networking.md](technical/networking.md))
- Crosshair customization settings (v1 backlog; the crosshair already reacts dynamically, just isn't user-configurable — tracked as F9 in [technical/PHASE-9-DESIGN.md](technical/PHASE-9-DESIGN.md))
- Google OAuth (v1 backlog)

### LOW

- Friends, parties, and achievements (v1 backlog — needs presence infrastructure, deliberately deferred, see [decisions.md](decisions.md))
- Industrial Factory / Desert Base map entries (v1 backlog)
- Controller support and localization (v1 backlog)
- Spectator mode (v1 backlog)

## Adding items

New items go under the tier that reflects actual urgency, with a link to whichever doc has the detail (or "no design doc yet" if it's a raw idea). Move an item to [history.md](history.md) once it ships.
