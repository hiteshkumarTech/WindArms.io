# Operator Production Pipeline — Phase 5 Report

**Status:** architecture COMPLETE (2026-07-17) · **First operator:** Operator 01 — Kael Aurin (male) · **No GLB exists yet** — Phase 6 delivers it. Nothing in V1 was touched; no gameplay was invented; the Vortex Rifle assets were not modified.

This is the production report the Phase 5 directive asked for: architecture summary, folder tree, extension points, known blockers.

## 1. Architecture summary

One operator definition feeds every consumer — FP hands, reload/inspect animations, lobby character, hero page, skins, emotes, killcam, third-person — through three layers, each replaceable independently:

```
CONFIG (pure TS, no React)         src/lib/v2/operators/
  OperatorDefinition = meta (canon roster ref) + visual + animation + attachments + skins
        │  identity is REFERENCED from content/operators.ts (canon), never copied
        ▼
RUNTIME (R3F, no DOM)              src/components/three/operators/
  OperatorModel — THE component: LOD resolve → skeleton clone → sockets →
  animation states → render modes → skins → silhouette fallback
  FirstPersonOperatorRig / OperatorPawn / OperatorSocketAnchor wrap it per context
        ▼
PRESENTATION (DOM + Canvas)        src/components/v2/operators/
  OperatorShowcase — hero lighting + turntable + pedestal, own <Canvas>
```

Everything rides the EXISTING asset pipeline (`src/lib/v2/pipeline/`) — slot probing, LOD fallback (`.lod1`/`.lod2`), Draco/WebP via `useGLTF`, manifest validation, accent tinting. Zero duplication; the pipeline's `SocketName`/`ClipName` unions were extended (per their own "extend, don't invent" rule) and two operator manifest entries added.

### The six requested configs

| Type | Home | Notes |
|---|---|---|
| `OperatorDefinition` | `operators/types.ts` + `registry.ts` | Composes the five below; `kael` + `veyra` registered |
| `OperatorMetadata` | `operators/types.ts` | References the canon `OperatorContent` (never forks it); adds gender + typed weapon id |
| `OperatorVisualConfig` | `operators/types.ts` | slot, fpArmsSlot, scale (=1 until measured), groundOffsetY, targetHeightM, showcase rim/rotation |
| `OperatorAnimationConfig` | `operators/types.ts` + `animations.ts` | 16 states × {clip, loop, fade, timeScale, returnTo}; `buildDefaultAnimationConfig()` |
| `OperatorAttachmentConfig` | `operators/types.ts` + `sockets.ts` | 16 typed sockets; per-socket node override / bone fallbacks / local offset |
| `OperatorSkinConfig` | `operators/types.ts` | rarity ladder default→mythic; accent-tint tier (free) + model-override tier; only `default` populated |

### Animation states (16) — no component rewrites

`idle walk sprint ads fire reload inspect equip unequip jump fall land death victory lobby_idle selection_pose` — a `Record` keyed by the `OperatorAnimationState` union. Adding a state = extend the union + two tables; the compiler surfaces every consumer. Components receive states via ONE prop (`animationState`). One-shots chain (`jump→fall`, `reload→idle`) via the mixer's `finished` event; missing clips no-op with a single dev warning (fail-soft, like the rest of the pipeline).

### Sockets (16, typed)

`head neck spine pelvis left_hand right_hand left_foot right_foot weapon_primary weapon_secondary back belt grenade camera_fp camera_tp muzzle_reference`

Resolution order: explicit config node → authored `socket_<id>` empty (pipeline convention) → humanoid bone fallbacks (Mixamo / UE mannequin / Rigify names) — so a stock autorig works day one. `camera_tp`/`muzzle_reference` have no bone fallback by design: author them or they stay unresolved. Required subset (validation + inspector): `head spine pelvis right_hand left_hand weapon_primary camera_fp`.

### First person

`FirstPersonOperatorRig` mounts under the camera: `eyeOffset → swayPivot → recoilPivot → OperatorModel` — pivots exposed via ref for future sway/procedural recoil (the range's existing `viewKick.ts` spring is the intended driver), children slot for the held weapon so body+weapon move as one. Body modes: `armsOnly` (mesh-name convention now; dedicated `operator-<id>-arms.glb` slot reserved for Phase 7), `fullBody`, `bodyHidden` (skeleton still animates → sockets track), `shadowOnly` (shadow-map-only material). All reversible at runtime — death cam can flip FP→TP without reloading.

### Third person

`OperatorPawn` context presets: `lobby→lobby_idle · killcam→idle · spectator→idle · victory→victory · mvp→selection_pose`. New context = one row. Future emotes = new animation states played through the same `animationState` prop.

### Model pipeline support

.glb ✓ (useGLTF) · Draco ✓ (drei auto-decoder) · WebP textures ✓ (EXT_texture_webp) · LOD0/1/2 ✓ (existing `.lodN` resolver + graphics-quality tier) · AnimationMixer ✓ (per-instance, `SkeletonUtils.clone` so two pawns of one operator never share a skeleton) · KTX2 — future (extension point: swap loader config in one place, `useGLTF`'s extendLoader, when we adopt it) · IK / facial / cloth — future (attach at the exposed skeleton via `OperatorModelHandle.object`; nothing rewrites).

## 2. Folder tree (new/changed only)

```
src/lib/v2/operators/            NEW — config layer (pure TS)
  types.ts                       the six config types + unions
  sockets.ts                     16 sockets, required set, bone fallbacks
  animations.ts                  16 states, clip names, playback policies
  assetSlots.ts                  slot naming: operator-kael[.lodN|-arms|-skin-<id>].glb
  registry.ts                    OPERATOR_DEFINITIONS (kael, veyra), DEFAULT_OPERATOR_ID
  index.ts
src/components/three/operators/  NEW — runtime layer (R3F)
  OperatorModel.tsx              THE operator component (+OperatorModelHandle)
  OperatorSilhouette.tsx         procedural fallback mannequin (accent-lit)
  OperatorSocketAnchor.tsx       typed socket mounting (reuses pipeline SocketAnchor)
  FirstPersonOperatorRig.tsx     FP mount: sway/recoil pivots, body modes
  OperatorPawn.tsx               TP contexts: lobby/killcam/spectator/victory/mvp
  renderModes.ts                 full/armsOnly/bodyHidden/shadowOnly (reversible)
  hooks/useOperatorAnimations.ts mixer + crossfades + one-shot chaining
  hooks/useOperatorSockets.ts    socket resolution (convention → bones)
  index.ts
src/components/v2/operators/     NEW — presentation layer (DOM+Canvas)
  OperatorShowcase.tsx           hero lighting, turntable, pedestal, error fallback
  index.ts
src/lib/v2/pipeline/types.ts     EXTENDED — SocketName +14, ClipName +8 (additive)
src/lib/v2/pipeline/manifest.ts  EXTENDED — operator-kael + operator-veyra entries, shared OPERATOR_BUDGET
tools/inspect-operator.mjs       NEW — character GLB gate (see §4)
WindArms Assets/Characters/      staging (create when the first source GLB arrives)
  Operator01_Kael/               → kael_v0.1_source.glb, bakes, textures
```

## 3. Authoring contract (what Phase 6's GLB must be)

Scale: 1 unit = 1 m, Kael height ≈ **1.83 m** (`targetHeightM`) — mismatches are corrected via `visual.scale` after measuring, never eyeballed. Origin at the **feet**, facing **-Z** (project rig convention, `pipeline/sockets.ts`). One armature, one skin, ≤120 joints web-budget. Socket empties named `socket_<id>` (16 above; 7 required). Clips named exactly like states (16; ship what exists, the rest warn). Materials named; the tintable one contains `accent`/`energy`/`tint` (skin-tint target). LOD0 ≤ **45k tris / 10 materials / 2048px textures** (matches `OPERATOR_BUDGET`), LOD1 ≤ 20k, LOD2 ≤ 8k, exported as `operator-kael.glb` / `.lod1.glb` / `.lod2.glb` into `public/v2-art/`. Arms model (Phase 7): `operator-kael-arms.glb`.

## 4. Validation

`node tools/inspect-operator.mjs <file.glb> [--lod 0|1|2]` — zero-dep gate: skeleton/joints, skinning coverage, per-clip channels+duration vs the 16 states, `socket_*` empties vs the 16 sockets (with "resolvable via bone fallback" downgrades), duplicate node names, materials/tintable check, tris/materials/file/texture budgets per LOD. Exits non-zero on errors (CI-ready). In-engine, the existing dev validator runs the manifest checks automatically on load.

## 5. Extension points (typed, functional, empty by design)

Skins beyond `default` (rarity ladder + two mechanisms ready); emotes (new states, same prop); hand IK / facial / cloth (skeleton exposed on the handle); dedicated FP arms GLB (slot reserved, rig prefers it when present); scripted showcase cameras (`cameraDirector` prop); showcase background scenes (`background` prop); KTX2; per-operator clip remaps (`buildDefaultAnimationConfig(overrides)`); abilities/gameplay stats (deliberately absent — no design exists; will follow `windWeapons.gameplayStats` optional-field precedent); server-side operator identity (graduates to `shared/` when V2 combat needs it, per `windWeapons.ts` precedent).

**Hand IK — implemented and mounted in BOTH `/v2/range` and `/v2/play` (2026-07-21, Phase F Step 6; extended to `/v2/play` 2026-07-22).** `src/lib/v2/operators/ik/twoBoneIk.ts` (pure analytic two-bone solver) plus `src/lib/v2/operators/kaelArmRig.ts` (bone resolution + rest-metrics, hand-basis correction measured independently per side — confirming in running code what `inspect-kael-hand-basis.py` proved on the static rest pose) connect the FP arms GLB (`operator-kael-arms.glb`) to the Vortex Rifle's published grip targets (`gripWorldPose.ts`) via `KaelFirstPersonArms.tsx`, a standalone component mounted identically (unmodified) in both `RangeScene.tsx` and `V2PlayScene.tsx` — extending to the second route required zero changes to the arms system itself, confirming the original route-agnostic design (`docs/decisions.md`). Both hands visibly track the weapon through hip-fire/ADS/movement/recoil/reload/inspect, in both scenes; VortexViewmodel remains the sole owner of the weapon transform. **Not visually calibrated** (no browser access this session — elbow-pole/shoulder-offset/finger-curl values are first-pass structural defaults, a `?ik=1` dev tuner exists in `/v2/range` to correct them; not mounted in `/v2/play`, matching the existing grip/muzzle debug-tool precedent of range-only calibration UI). The grip-target VALUES this IK layer solves toward remain final (visually calibrated `/v2/range?grips=1`, 2026-07-21): right hand `position: [-0.25, -0.065, 0.0]` / `rotationEuler: [0.0, 0.0, -1.1519]` (-66° Z); left hand `position: [0.22, -0.05, 0.0]` / `rotationEuler: [0.0, 0.0, -0.5061]` (-29° Z), both `rotationOrder: 'XYZ'`, both temporary runtime proxies not authored GLB sockets. Full detail: `docs/design/weapons/vortex-rifle.md` §22a–§22b.

**Visibility blocker fixed 2026-07-22.** The arms mesh was invisible at runtime (mount/IK were both working, but the asset's skeleton is authored in full-body character space, not shoulder-relative — see `docs/decisions.md`'s "Kael FP-arm visibility blocker" entry for the full root-cause trace and fix). Verified structurally (unit tests + a headless real-asset simulation); not yet confirmed in an actual browser.

**`operator-kael-arms.glb` rebuilt 2026-07-22 (Step 6C) — "exploded geometry" traced to a Decimate-modifier artifact, not the extraction method or the IK runtime.** After the shoulder-offset fix above, manual browser testing still showed giant polygon slabs. Real Blender automation against the accepted rigged source found the actual mechanism: the existing `tools/blender/make-kael-fp-arms.py` pipeline's Decimate step protects hand/finger vertices from reduction but has zero protection at the open shoulder/torso cut boundary — Collapse decimation next to that open edge stretched individual triangles to ~0.12–0.16m (max face area 104x normal) even though the raw pre-decimation extraction never exceeds ~0.02m anywhere. Fixed with a new post-decimation cleanup step (delete any face whose longest edge exceeds 0.035m, then clean up loose vertices) — measured on the real asset: max edge 0.1615m → 0.0344m, max face area 0.008674 → 0.000318 m² (27x). A genuine independent bug in the same script (`proximal_bones` silently resolving to almost nothing since Step 4, due to a dict-nesting mistake) was found and fixed along the way. `tools/inspect-operator.mjs --mode arms` gained a permanent max-edge-length check so this defect class is caught at the inspector gate going forward, not just by a human looking at a browser. Full investigation, rejected alternatives, and measurements: `docs/decisions.md`. Runtime compatibility (bone resolution, recentering, a 20-frame IK simulation) confirmed against the rebuilt asset; not yet confirmed visually.

**Debug tooling fixed 2026-07-22 (Step 6D) — the `?ik=1` tuner's own bounding-box overlay was never measuring the deformed mesh; a harder "true rest pose" diagnostic was added.** Manual browser testing after the Step 6C rebuild reported the rebuilt arms still unusable — completely invisible with `IK disabled` on, and the bounding-box helper drawing enormous magenta lines across most of the screen. Headless investigation found and fixed one confirmed bug: `ArmBoundingBoxHelper` used `THREE.Box3.setFromObject()`, which reads each mesh's static `geometry.boundingBox` (computed once at load from the undeformed vertex attributes) — GPU skinning happens entirely in the vertex shader and never touches that CPU-side data, so the box was identical regardless of IK weight or pose. Replaced with `computeDeformedSkinnedBounds()` (`kaelArmSolve.ts`), which walks every vertex through `SkinnedMesh.applyBoneTransform` — the same math the GPU actually runs — verified against the real rebuilt asset (`[1.009, 0.745, 0.195]` m, finite, arm-scale) and against a new synthetic 2-bone unit-test mesh that visibly moves when rotated (the static method never would have). Separately, since `IK disabled` only *converges toward* rest pose via the solver's own weight=0 blend branch rather than hard-bypassing it, a genuinely solver-free `restoreRestPose()` was added — captures each bone's transform once at mount, restores it verbatim on demand, with test coverage for exact restoration, 100-cycle no-drift, and finger-pose removal. Exposed as a new, separate **"REST MESH DIAGNOSTIC"** toggle in the `/v2/range` tuner panel, alongside (not replacing) the existing "IK disabled" toggle. Whether a genuine invisibility bug remains beyond the now-fixed bounding-box display bug is still an **open question** — headless simulation against the real asset produces sane results, but "completely invisible in-browser" could not be reproduced without a browser. Full investigation and reasoning: `docs/decisions.md`'s 2026-07-22 "bounding box was measuring the wrong thing" entry.

**Resolved 2026-07-23 (Step 6E) — "invisible in REST MESH DIAGNOSTIC" was a viewing-angle mismatch, not a bug.** Manual testing after the above fix still reported invisible arms with an oversized/detached bounding box. Built `classifyVerticesInCameraSpace()` (`kaelArmSolve.ts`) — true camera-space/NDC frustum classification of every deformed vertex, the same math the GPU rasterizer uses — and ran it headlessly against the real rebuilt asset through every stage of the transform chain. Result: with the shipping `shoulderRootOffset`, the arm's rest pose (dangling at the sides, authored for third-person viewing) places only 0.1% of vertices inside the visible frustum from this close a first-person camera — it sits almost entirely below the screen. The SAME measurement at the actual shipping IK weights (0.25/0.5/1.0) shows the arm correctly entering frame as it's pulled toward the grip target (0.1% → 36.3% → 66.7%). Four other explanations were tested and disproven by direct measurement: backface culling (material is `THREE.DoubleSide`), camera render layers (none used anywhere in `src/components/three`), a second/dedicated viewmodel camera (`VortexViewmodel.tsx` has none), and the debug helper double-applying the camera transform (read `Box3Helper`'s three.js source directly — it's mounted at scene root with an identity parent, mathematically correct as-is). Two small unrelated bugs were fixed along the way: a debug-only bone-`matrixWorld` staleness window (never affected the real rendered frame), and a three.js gotcha in the newly-built "DIRECT CAMERA MOUNT" diagnostic where objects parented directly to R3F's default camera don't render at all unless the camera is explicitly added to the scene graph — caught and fixed before it shipped. Conclusion: the transform/recenter/skinning/IK chain was already correct; the acceptance check going forward is the IK-enabled shipping configuration, not the rest-pose diagnostic. Full investigation: `docs/decisions.md`'s 2026-07-23 entry.

**Real-browser motion validation, 2026-07-24 — PASS.** The Step 6H/6I approved pose was checked via actual Playwright/Chromium screenshots (not headless math) through hip-fire, ADS, a full 30-round magazine fired while ADS, and ADS release, at the shipped values with no slider changes. No hand detachment, wrist snapping, left-hand-through-barrel, exploded triangles, scope obstruction, or elbow flipping. One pre-existing unrelated console 404 (`vortex-rifle.sfx-fire.mp3`) confirmed and attributed, not a new defect.

**Materials shipped 2026-07-24 (Step 7A) — realistic PBR gloves/sleeve/armor replace the flat neutral-gray dev material; no IK/grip/weapon values touched.** `operator-kael-arms.glb` gained a real material: dark charcoal tactical gloves, pale cloud-grey sleeve trim, titanium/gold-fleck armor accents, and a restrained cyan seam accent, baked into three 1024px textures (BaseColor/Normal/packed-ORM) via a new `materials` mode in `tools/blender/make-kael-fp-arms.py` that takes the already-accepted, already-IK-calibrated GLB as pure input (the extraction/decimation/scalpel pipeline itself was never re-run). An audit found the mesh is almost entirely hand/finger geometry with no natural forearm-shaft surface (glove region: 10,311 of 10,512 verts) — region thresholds and the new shoulder-cut cuff's material assignment were set from this real distribution, not an assumed even three-way split (full reasoning: `docs/decisions.md`). Four real bugs were found and fixed in the new pipeline script itself before trusting its output (a silent `Image.save()` no-op on relative paths, a dead duplicate-node approach to `occlusionTexture` that produced no occlusion texture at all, a missing sRGB→linear color decode that washed out every baked color, and stray vertex-color data leaking from a bmesh round-trip) — see `docs/decisions.md` for all four. Final asset: 19,727 tris / 10,515 verts, 1 material, 3 textures, 1.02 MB; bone names/count and skin weights confirmed byte-identical to the pre-materials asset (zero IK-calibration risk). `tools/inspect-operator.mjs --mode arms` extended with material-extension/texture-dimension/UV-normal-tangent-finite/unweighted-vertex checks (0 errors, 0 warnings on the shipped asset). Real-browser (Playwright) screenshots on `/v2/range` and `/v2/play` confirm the material reads correctly in both dark and bright lighting, no shader errors, no GLB/texture 404s. Full detail: `docs/changelog.md`'s 2026-07-24 entry, `docs/decisions.md`.

**Reload/inspect action-pose presentation shipped 2026-07-25 (Step 7C) — the left hand now visibly releases toward a temporary weapon-relative target and returns exactly to the calibrated grip during reload and inspect; no IK/grip/weapon/material values touched.** Procedural (not authored-clip) — a pure `computeActionPose()` function (`lib/v2/operators/actionPose.ts`) drives both the weapon's own reload-dip/inspect-presentation motion and the left hand's IK-target blend, computed once per frame in `VortexViewmodel.tsx` and read by `KaelFirstPersonArms.tsx` through a new bridge (`actionPoseState.ts`). The right hand and the existing `solveSide` solver are completely unmodified — the left hand's target is blended OUTSIDE the solver, before it's ever passed in. New weapon-local action-target anchors (`lib/v2/weapons/actionTargets.ts`) are small, honest offsets from the already-calibrated grip anchor (the real mesh has no separate magazine geometry to target). A new `?anim=1` dev preview tool (play/scrub/freeze reload+inspect, 3D action-target marker) supports a future human calibration pass on those two anchors. 31 new unit tests plus real-browser validation: both actions confirmed cycling through their real phases end-to-end, 5 consecutive reloads and 5 consecutive inspects with zero drift, a full interruption matrix, and `/v2/play` pause proven to freeze the pose byte-for-byte identical across a real 1-second gap. Live `?ik=1` re-check: still RIGHT 0.00cm / LEFT 0.00cm. A genuine, pre-existing, unrelated bug in `VortexFireSystem.tsx` (a queued trigger press during reload isn't cleared on the reload-blocked exit path, firing a phantom shot the instant reload completes) was found by this pass's testing and reported, not fixed, per this pass's own scope limits. Full detail: `docs/design/weapons/vortex-rifle.md` §22c, `docs/changelog.md`'s 2026-07-25 entry, `docs/decisions.md`.

**Shadow architecture audited and its first dev-only infrastructure shipped, 2026-07-28 (Steps 8E-A/8E-B, COMMITTED `a5967fe`) — the `shadowOnly` render mode listed under "First person" above finally has a real consumer.** Step 8E-A (read-only) compared four full-body-shadow architectures against the now-landed `operator-kael.lod1.glb`; Option B (full-body LOD1 shadow rig, legs locomotion-synced, arms IK-solved to the real grip target, separate shadow-only weapon) was approved, but its own literal proposed next step was rejected — the full-body GLB has zero animation clips, so disabling the existing FP-arms shadow in favor of an un-posed relaxed-A-pose full body would have shipped an obviously wrong rifle-user silhouette. Step 8E-B (`/v2/range?shadow=1` only, never production) built ONLY the non-visual-facing half of Option B: `KaelFirstPersonShadowBody.tsx` mounts one `shadowOnly`-mode clone of `operator-kael.lod1.glb`, its legs driven by the EXACT SAME locomotion computation as the visible lower body (a new generation-safe bridge, `lowerBodyLocomotionPoseBridge.ts`, structurally prevents the two skeletons' stride phases from ever drifting apart, rather than relying on two call sites staying in sync by convention), positioned via a physically-derived root offset (`shadowBodyTransform.ts` — deliberately NOT the visible body's Step 8C.1 silhouette-tuned offset, since that fix targets a first-person axial-viewing-angle problem a shadow doesn't have). Arms/spine remained the static bind pose at this stage. A real, independently-discovered `/v2/range` shadow-camera-frustum bug (THREE's default `±5` bounds never covered the range's actual floor) was fixed in the same pass.

**Arms solved, weapon shadow added, same day, 2026-07-28 (Step 8E-C) — the full-body shadow now visibly holds the rifle with both hands instead of standing in the static bind pose. Human review DECLINED — see Step 8E-C.1 below.** `shadowUpperBodyRig.ts` adds world-space two-bone IK for both arms plus a restrained spine/chest assist, reusing the visible arms rig's already-generic bone-resolution/rest-metrics/IK-solve machinery (`resolveKaelArmBones`/`measureArmRestMetrics`/`solveTwoBoneIk`) but with entirely its own config and debug state. A separate `KaelFirstPersonShadowWeapon.tsx` follows an ALREADY-EXISTING canonical weapon-world pose (`gripWorldPose.ts`'s `weaponWorldPosition`/`weaponWorldQuaternion`, published since Milestone 7 Phase F Step 5) rather than a newly-built bridge. Real-browser calibration got both hands to 0.00-2cm error at a natural ~30°+ downward-aim pitch, but reach quality degraded up to ~17cm at level pitch (a genuine, characterized, but player-visible limitation) — the human reviewer correctly judged this NOT acceptable as a stopping point. Also corrected a real Step 8E-B reporting error: that step's own report claimed the visible FP-arms' shadow was already being disabled during a debug session — it wasn't (the relevant file was never touched by that commit) — now genuinely wired.

**Pitch-invariant fix, same day, 2026-07-28 (Step 8E-C.1) — the level-pitch reach failure is fixed with a real, measured architectural correction, not a bigger caveat.** A genuine cascading Spine→Spine1→Spine2 chest-aim frame (`applyChestAimFrame`, solved BEFORE the arms — arm rest-metrics now measured relative to the chest bone, not the yaw-only root) plus a dynamic, pitch-aware, bounded, smoothed per-side shoulder assist (replacing Step 8E-C's fixed constant) together close the gap. A real sign-inversion bug was caught by live measurement (the first "corrective" lean made the left hand WORSE, not better) and fixed with a permanent regression test. The final chest-lean-vs-assist split was measured, not assumed: even an anatomically-extreme 34° chest lean alone only closed a fraction of the ~38cm raw gap (the shoulder's own lever-arm from the chest pivot is short), so a restrained 25° lean plus a larger, still-bounded per-side assist cap (`leftMaxShoulderAssistM: 0.4`) does the rest. Full pitch×yaw matrix (0-70°×5 yaws, properly settled) now measures ≤0.34cm everywhere; a real recoil transient peaked at 0.78cm. Human review DECLINED anyway — see Step 8E-C.2 below for why the numbers passing wasn't the whole story.

**Chest-anchored weapon presentation, 2026-07-29 (Step 8E-C.2) — the numbers from 8E-C.1 were real, but achieving them required up to 40cm of shoulder translation from a target that was never anatomically reachable in the first place. Fixed the architecture, not the cap.** The human reviewer's own diagnosis: the camera-relative weapon target 8E-C/8E-C.1 solved toward is "intentionally camera-friendly, not necessarily reachable by a physically proportioned world-space character." `shadowWeaponPresentationPose.ts` (new) anchors the shadow weapon's position to the chest bone's own live transform instead — reachable by construction, since it's a small fixed offset from a bone the arms already solve relative to — with grip/action anchors re-resolved against it via the same already-tested `resolveRuntimeAnchorWorldPose`. The chest-lean curve, freed from also having to solve reach, changed from Step 8E-C.1's reach-driven inverse shape to a genuine monotonic follow of aim pitch. Building this pass's validation ALSO surfaced a real, previously undiagnosed bug: `extractAimPitchFromWeaponQuaternion` reads the weapon's own fully-composed quaternion, which (because of the weapon's own ~90° model-forward-correction rotation) does not track camera pitch linearly — verified with a standalone math script to under-report a 30°/60°/88.9° camera pitch as only ~2.4°/4.3°/5.1°, the EXACT numbers the reviewer's own complaint cited. Fixed by reading `rangeLocalPose.pitch` directly instead. **Result**: the full pitch×yaw×ADS×action-state matrix now measures 0.00cm error / 0.00cm shoulder assist / zero reach-clamping everywhere; shoulder-assist caps dropped to a symmetric 6cm (from 10/40cm) with a dev-only over-8cm warning. Elbow-pole/hand-basis values are UNCHANGED this pass (inherited, not re-tuned), and a systematic camera-angle hunt found no marker-free view of the actual ground-projected shadow is achievable from the current first-person-only camera (geometrically ~1.2m shadow length given the range light's elevation, occluded by the character's own body at any angle) — flagged openly rather than worked around by adding new camera tooling outside this step's scope. Human review DECLINED again anyway — see Step 8E-C.3 below.

**External review camera + real per-action presentation, 2026-07-29 same day (Step 8E-C.3) — the human reviewer explicitly authorized a dev-only spectator camera this time ("an isolated validation camera that never ships"), directly answering 8E-C.2's own "no marker-free view possible" finding.** `KaelShadowReviewCamera.tsx` (new, `?shadowReview=1`) is a genuinely separate `THREE.PerspectiveCamera`, built via R3F's manual-render mechanism (`useFrame(callback, 1)`) so it never touches `RangeController.tsx` or the real gameplay camera — the real camera keeps updating from real input the whole time, unaware the review camera exists; disabling review mode requires no explicit restore code, verified live. Seven fixed presets orbit the player's own real body position. A new neutral matte receiver (`KaelShadowReceiver.tsx`) makes the projected shadow itself visible. Separately, `SHADOW_WEAPON_PRESENTATION` gained 7 new named per-action poses (sprint/reload×3/inspect×3, plus a decaying recoil kick) — before this pass, every action state produced the byte-identical hip-fire weapon transform, confirmed by direct audit. Building the new camera exposed two real bugs 8E-C.2's reach-only validation structurally could not catch (an IK solver can't tell "behind" from "in front," only distance): the weapon's chest-local "forward" offset was pointing at the spawn wall, not the range interior, and the weapon's diagnostic-visible material rendered black against the scene's lighting. Both fixed and verified empirically. Full 15-state reach matrix re-verified with the new poses: still 0.00cm everywhere. Elbow-pole/wrist-basis remain visually plausible but not re-tuned. Full detail: `docs/changelog.md`'s 2026-07-29 (later) entry, `docs/decisions.md`.

**Isolated shadow review + final anatomy sign-off, 2026-07-29 same day (Step 8E-C.3.1) — the human reviewer approved the 8E-C.3 architecture outright but rejected its visual gate on concrete, evidence-based grounds: the visible FP lower body was STILL rendering during review mode (a render-grouping oversight, not a design flaw), the weapon wasn't clearly readable, and marker-free shadow shots were too small to verify a complete silhouette.** Fixed the lower-body contamination by moving `KaelFirstPersonLowerBody` inside the SAME `<group visible={!shadowReviewEnabled}>` the viewmodel/arms were already hidden by — it had simply been left outside that group in 8E-C.3. Added 6 new close/medium camera presets (`bodyCloseThreeQuarter`/`bodyCloseSide`/`handsCloseRight`/`handsCloseLeft`/`shadowClose`/`shadowWide`) and captured the full 13-state marker-free shadow matrix the brief required. Re-inspected elbow-poles/wrist-basis across all the new close angles (front/rear/side, hip-fire/ADS/sprint/reload/inspect) and CONFIRMED them anatomically correct — no crossing, locking, backward bend, or flare — so, per the brief's own instruction not to change poles merely to create activity, they were left unchanged. Fixed a real diagnostic-material contrast problem (the weapon's fill read fine against the dark background but blended into the body's OWN pale diagnostic material). **A near-miss, recorded honestly rather than glossed over**: this pass nearly re-flipped `WEAPON_CHEST_ALIGN_QUAT` based on a side-view visibility comparison that turned out to be direction-blind (can't tell forward from 180°-backward); caught before shipping by a genuinely direction-sensitive muzzle-tip-vs-pivot-vs-chest position check, which confirmed the existing Step 8E-C.3 value was already correct. Weapon readability remains angle-dependent even with orientation confirmed and material fixed — clear from oblique/elevated cameras, occluded by the character's own forearms from a pure side profile — disclosed rather than further chased, since resolving it further would mean tuning weapon position, not camera work. Full detail: `docs/changelog.md`'s 2026-07-29 (Step 8E-C.3.1) entry, `docs/decisions.md`.

**Camera-preset front/rear/left/right naming fixed, 2026-07-30 (Step 8E-C.3.2, committed `128732f`) — a tiny, isolated, root-caused fix, no architecture touched.** The yaw-relative presets measured their offset from a base vector pointing world +Z, but the player/character faces world -Z at yaw=0 — confirmed two independent ways before touching code. The fix negates only the front/back component (`dir.z = -dir.z`), preserving the already-correct left/right mapping (verified via the real R/L shoulder world-position readout before shipping, avoiding a would-be regression a naive full-vector negation would have introduced). Full detail: `docs/changelog.md`'s 2026-07-30 entry, `docs/decisions.md`.

**Dev-only shadow visual/light calibration, 2026-07-31 (Step 8E-D, committed `df632ca`) — the repository's own 4-step sequence names this step "visual/light calibration," distinct from 8E-E's "final caster-ownership switch"; production shadow ownership was NOT touched.** Added `production`/`readable` receiver modes (`KaelShadowReceiver.tsx`) — `production` imports the real floor's own `STORM.abyss` token rather than a re-typed hex value, and revealed a genuine, previously-unseen finding: the real floor is dark enough that a cast shadow reads with low contrast against it, a pre-existing characteristic of the production floor, not a bug this step introduced. Shadow-bias/normalBias (tested across a 10x range) and shadow-map resolution (1024² vs 2048²) both produced NO visually detectable difference — confirmed as a genuine finding (not an insensitive test — the receiver-mode change WAS clearly visible in the same screenshots) and traced to `RANGE_SHADOW_CAMERA_BOUNDS`' frustum spanning the entire floor, making texel count a non-bottleneck at the character's scale. Canonical calibration therefore matches the pre-8E-D production defaults exactly — shipped as an honest negative result rather than an unverified "reasonable-looking" number, deliberately avoiding a repeat of the Step 8E-C.3.1 near-miss's own lesson. Self-shadowing tested the same evidence-gated way, also no visible effect, left disabled. Full detail: `docs/changelog.md`'s 2026-07-31 entry, `docs/decisions.md`.

**Player-centered, texel-stabilized shadow frustum, 2026-07-31 (Steps 8E-D.1A measurement + 8E-D.1 implementation) — directly acts on 8E-D's own architectural finding that the full-floor frustum, not bias/resolution, is the real texel-density bottleneck.** 8E-D.1A measured the REAL deformed mesh (`SkinnedMesh.applyBoneTransform`, not rest-pose bounds) across 13 action states and approved a 3.5m×6m rectangular frustum (near 20/far 27) — tighter than any square candidate, continuously texel-snapped with no dead zone (a 1-2 texel dead zone was measured to provide negligible benefit at this scale while risking real clipping margin). 8E-D.1 implements it: a pure, dependency-light snapping module (`playerCenteredShadowFrustum.ts`), a fixed light-space reference frame (`playerCenteredShadowFrustumBasis.ts`) that must use a `THREE.Camera` rather than a plain `Object3D` — a real, easy-to-miss three.js `lookAt()` convention difference (camera/light objects vs. plain objects orient with OPPOSITE handedness) that was caught by a round-trip unit test asserting exact target-projection geometry, not by visual inspection (a mirrored-but-self-consistent basis would have looked like it was working). The imperative controller (`KaelPlayerCenteredShadowController.tsx`) tracks the player's ground anchor at a FIXED Y (the canonical light target's own original Y, never the player's live/airborne Y — confirmed live: a full jump/fall/landing arc left the light rig's own Y exactly constant) and is the sole owner of the light/target positions and shadow-camera bounds while mounted, restoring canonical values unconditionally on unmount regardless of which mode was active. An always-present explicit target `Object3D` (rather than conditionally attached) keeps normal `/v2/range` byte-identical to before this pass while also guaranteeing no stale translation survives a flag disable. 30 new tests (479/479 total). Extensive real-browser validation confirmed correct tracking across all movement/jump/sprint/state-transition cases, review-camera isolation (the external observation camera cannot influence the tracked frustum), and exact static-mode restoration; one HEADLESS-ENVIRONMENT-ONLY finding was disclosed rather than hidden — the dev debug-panel's own readout occasionally polls up to ~2.5s stale under Playwright's headless Chromium (the underlying store/controller state itself switches instantly; only the QA-convenience readout poll lags in this specific sandbox). A temporary performance probe (added, measured, fully removed) found draw-call/triangle/texture counts identical across static-1024/player-centered/static-2048 configurations, as expected (frustum/resolution changes don't add scene geometry) — absolute frame-time numbers were software-rendering sandbox noise, not reported as a real-hardware claim. The shadow-sharpness visual verdict itself (texel density improves ~37x/~21x per axis by construction) is left to human review, not claimed by Claude. Full detail: `docs/changelog.md`'s 2026-07-31 (Step 8E-D.1) entry, `docs/decisions.md`.

**Production shadow-caster ownership switch, 2026-08-01 (Step 8E-E, Option 1 — range-only, COMMITTED `94169ad`) — gated on its own live-motion shimmer prerequisite (Step 8E-E.0, PASS WITH WARNING) before implementation began.** The full-body body+weapon shadow system (`KaelFirstPersonShadowBody.tsx`/`KaelFirstPersonShadowWeapon.tsx`/`KaelPlayerCenteredShadowController.tsx`) becomes the production shadow caster in `/v2/range`, with the visible FP arms' own shadow contribution turned off — behind one typed, source-controlled policy (`shadowCasterPolicy.ts`, `RANGE_SHADOW_CASTER_POLICY: 'fp-arms' | 'full-body'`, canonical `'full-body'`, one-line rollback). A pure `resolveRangeShadowCasterDecision()` function is the single source of truth for exclusive ownership, exhaustively unit-tested across every combination of the existing dev flags and the new policy — the same dev-only components now serve BOTH the production caster and the `?shadow=1&shadowReview=1` review harness through one shared mount site, never a second implementation or a second clone. `/v2/play` is unaffected by construction (not just by convention): `KaelFirstPersonArms.tsx`'s new `casterPolicy` prop defaults to the pre-8E-E behavior, and `V2PlayScene.tsx` never passes it at all — enforced going forward by a source-text regression test. A real cross-session leakage risk was found and fixed BEFORE any browser testing: `shadowReviewStore`'s `frustumMode` field is a module singleton that survives route navigation (by design, for the dev review harness), so a stale `'static-full-floor'` value left over from an earlier dev session could otherwise silently leak into a later flag-free production session in the same browser tab — fixed with a new `allowDevModeOverride` prop that makes the production activation path never read that store at all. Full real-browser validation confirmed zero debug UI in a flag-free production session, exactly one shared shadow clone regardless of activation reason, clean 5x route-remount cycles, and working two-directional rollback. Performance: the full-body clone is measurably higher-poly than the FP arms (+72,828 triangles, +2 draw calls — clean, deterministic, environment-independent) with frame-time deltas that fell in an informational band under this specific headless sandbox, not claimed as a real-hardware result. Range-only at the time — `/v2/play`'s own death/respawn/restart lifecycle had not yet been validated against this shadow system; see Step 8F below for that rollout. Full detail: `docs/changelog.md`'s 2026-08-01 (Step 8E-E / Step 8E-E.0) entries, `docs/decisions.md`.

**Full-body shadow-caster rollout to `/v2/play`, 2026-08-01 (Step 8F, not yet committed) — gated on its own play-specific measurement prerequisite (Step 8F.0, READY FOR STEP 8F IMPLEMENTATION) before implementation began.** Range's own near/far (`20`/`27`) could not simply be reused — play's light-to-target distance differs measurably from range's, and Wind Lift (no range equivalent) can carry the body mesh to roughly 10.6m of world-space elevation. Step 8F.0 measured the real per-frame light-space footprint across 140 captured frames (normal movement/combat/Wind Lift's complete rise-apex-fall arc/death/respawn/restart/pause) and found a global worst case of 2.557m width / 9.427m height (both Wind Lift) / near-floor 25.916 / far-ceiling 42.043, approving a `3.5×12m / near 24/far 44 / 1024²` configuration — deliberately NOT range's `3.5×6 / near 20/far 27`. Step 8F then generalized `shadowCasterPolicy.ts`'s exclusivity resolver from range-only to route-agnostic (`resolveShadowCasterDecision()`, no route name inside it, two independent policy constants so flipping one route's rollback can never affect the other) and parameterized `KaelPlayerCenteredShadowController.tsx` to take a caller-supplied light/target/frustum/anchor configuration instead of hardcoded range constants — range's own production and `?shadow=1&shadowReview=1` review-harness behavior proven byte-identical via full real-browser regression before the play switch was even attempted. `/v2/play` mounts the SAME shared shadow-body/weapon/controller components range already uses (no play-specific copies), with no dev review UI on this route this milestone (`allowDevModeOverride` unconditionally `false` — the decision reduces to a pure function of the policy constant alone). Full real-browser validation: all normal states clean; a real captured Wind Lift arc (teleport-assisted entry, real physics for the actual rise/fall) reached a ~9.3m peak matching the 8F.0 measurement, then fell and landed on the platform, with no clipping/popping observed; death/5x-restart/pause/5x-remount all showed zero resource-ownership accumulation (scene object counts stayed at exactly 1/1/1/1/1 throughout every cycle); the rollback gate confirmed exact restoration of both the static (`±30/1-80/2048²`) and player-centered (`±1.75/±6/24/44/1024²`) shadow-camera configurations by reading the live `light.shadow.camera`, not just component-mount counts; the texel-stability gate found zero unexplained shimmer (stationary and mouse-look sequences were byte-identical across every sample; one naive detector false-positive during rapid direction reversal was investigated and found to be correctly-explained real strafe motion, not spurious jitter — see `docs/decisions.md`). Performance delta (+2 draws/+72,828 triangles/+1 texture/+2 programs) exactly matches Step 8E-E's own range rollout, as expected for the same shared components now mounted on a second route. 500/500 tests, `typecheck`/`lint`/`build` clean, GLB hashes unchanged, V1 `/play` untouched. Full detail: `docs/changelog.md`'s 2026-08-01 (Step 8F) entry, `docs/decisions.md`.

**Drone AI 2.0 — Step 9A audit + Step 9B pure-core extraction, 2026-08-02 (not yet committed) — a different subsystem from everything else in this file (drones are procedural enemy geometry, not an operator/character asset), recorded here only because this milestone's own instruction scopes documentation updates to this file plus changelog/decisions/todo.** Step 9A audited the existing `/v2/play` drone system (`DroneEnemy.tsx`/`DroneSquad.tsx`/`DroneBoltPool.tsx`) end-to-end and designed the Drone AI 2.0 roadmap — READY WITH PREREQUISITES, no blockers beyond normal phase sequencing. Step 9B implements the recommended first phase: the drone system's existing five-state decision logic (`spawning/searching/engaging/attacking/destroyed`, with a timed stun overlay) moved out of `DroneEnemy.tsx` into a new pure, renderer-independent core (`src/lib/v2/ai/` — `droneAiTypes.ts`/`droneAiRandom.ts`/`droneAiStateMachine.ts`), with a deliberately **zero-intended-behaviour-change** scope — a captured real-browser pre-refactor trace (deterministic random tape, one drone driven through 16 scripted scenarios) both grounded the extraction and confirmed several precise legacy quirks now preserved exactly (fully-3D approach/retreat movement, a stale — not cleared — `windupUntil` after an aborted attack, and a restart that reseeds only the fire-cooldown jitter, never hover-phase/strafe fields). `DroneEnemy.tsx` is now a thin adapter; `DroneSquad.tsx`/`DroneBoltPool.tsx` are untouched. **Step 9B.1 (same day) closed three evidence gaps before commit**: reconciled a wrong test-count report, added an independent oracle cross-checked against the real core through a deterministic synthetic timeline (zero unexplained mismatches), and added a dedicated movement-parity suite (all deltas matched exactly). **Steps 9B/9B.1 committed together as `8710998`, 635/635 tests at commit time.** **Step 9C adds the first intentional, disclosed player-facing behaviour change**: a drone that loses line-of-sight after confirmed contact now briefly `investigating`s the last known position (moves toward it, cannot fire) before reacquiring or timing out to ambient searching — a new pure perception module (`droneAiPerception.ts`) reuses the existing occlusion algorithm; a new `DroneTargetSnapshot` contract replaces the old bare player-position parameter; player-death/respawn correctly invalidates stale memory. **Step 9C.1 (2026-08-03) closed four human-identified validation gaps** (exact file-count reconciliation, a real cover-geometry test against production perception code, a genuine real-physics Wind Lift arc, and the complete five-restart/five-remount/difficulty-switch lifecycle matrix) before commit. **Steps 9C/9C.1 committed together as `d1ef01b`, 692/692 tests at commit time.** **Step 9D (2026-08-03) extracts pure movement-intent + adds deterministic local separation**: a new `droneAiMovementIntent.ts` consumes the already-decided movement mode and blends in a bounded, XZ-only local-separation correction (proven byte-identical to every pre-9D formula with no neighbours present) so drones stop stacking into one visual blob while their existing approach/retreat/strafe/search/investigate behaviour stays unchanged; `DroneSquad.tsx` gained a two-pass spatial-snapshot update so separation is order-independent. **768/768 tests**, quantitative overlap comparison (identical moving-player methodology) showed a full elimination of sustained overlap in a single-run capture (min XZ 1.06m→1.58m, sustained-overlap time 473ms→0ms), full real-browser validation clean, zero console errors. **Step 9D.1 (2026-08-03) closed four human-identified gaps**: a measured speed inflation in the original separation blend (the acceptance gate requires none — fixed after five iterations, landing on a bounded, proximity-scaled CLOSE-PROXIMITY COLLISION-RECOVERY allowance that only activates once a neighbour is already visually touching, fully closing the overlap gap: 0% below threshold, 0ms sustained overlap, 2.33m minimum distance — better than both the 9C baseline and 9D's own original result); real (not forced) two-drone convergence and a real live 3-drone investigating cluster newly proven live; the full 9D-specific lifecycle matrix (5 restarts/5 remounts/difficulty 8→5→8) completed. **787/787 tests. Steps 9D/9D.1 committed together as `04035e6`.** Full detail: `docs/changelog.md`'s 2026-08-02/2026-08-03 (Step 9B / 9B.1 / 9C / 9C.1 / 9D / 9D.1) entries, `docs/decisions.md`.

**Step 9E (2026-08-04) adds combat cadence, telegraphing, and typed difficulty profiles.** A new `droneAiDifficulty.ts` module extends (never duplicates) the existing `ResolvedDroneConfig` (`lib/v2/play/difficulty.ts`) with `acquireReactionDelayMs` (Low 350ms, Medium 0ms — byte-identical cadence to every prior phase, Max 0ms), `targetMemoryDurationMs` (Low 3500ms, Medium 4500ms — reuses the existing 9C `DRONE_PERCEPTION_MEMORY.investigateDurationMs` constant verbatim rather than restating it, Max 5250ms), and `attackWindupMs` (always exactly `DRONE.WINDUP_MS`, 650ms, for every difficulty — absolute, not tunable). `droneAiStateMachine.ts` gained one minimal, surgical addition: a `reactionReadyAtMs` runtime field, seeded only on a genuine `searching`/`investigating`→`engaging` acquisition (never on a post-fire or post-stun-abort re-entry to `engaging`, never on a sub-confirmation cover-edge flicker), gating only the windup-START check — Medium's own same-tick acquire→windup cascade is completely unaffected since its delay is always 0. A new pure `droneAiTelegraph.ts` resolver (`idle|acquire|reaction|windup|fire|cooldown|hit|destroyed` phases, priority `destroyed > hit > fire > windup > acquire/reaction > cooldown > idle`) drives the drone's existing eye-emissive cue (now readable acquire→windup→fire→cooldown, not just the old flat idle/attacking/hit ternary) plus a new preallocated muzzle-flash visual — zero per-shot allocation, no light/shadow/physics/`setTimeout`, and structurally decoupled from the pure state machine (zero import in either direction, enforced by a guard test). **872/872 tests** (+85 since 9D.1's own 787), `typecheck`/`lint`/`build` all clean. Real-browser validation confirmed the resolved profile propagates correctly end-to-end (`DroneSquad.tsx`→`DroneEnemy.tsx`) and that the reaction-gate timing is exact live (`reactionReadyAtMs === acquiredAt + 350` for Low, `=== acquiredAt` for Medium/Max, captured via an in-page synchronous event log after external polling proved unreliable in this environment's slow headless rendering — see `docs/decisions.md`).

**Step 9E.1 (2026-08-04, not yet committed) closes five human-identified validation gaps before Step 9E can be committed** — an incomplete restart gate, no live proof of the rendered telegraph/muzzle-flash pipeline, the two new 9E lifecycle fields (`reactionReadyAtMs`, the adapter's presentation deadlines) never rechecked across a real player death/respawn, Wind Lift never re-run, and renderer-resource stability resting on a CPU microbenchmark alone. **Found and fixed one real, pre-existing (not-9E-caused) defect during this pass**: the new muzzle-flash mesh rendered on the drone's FAR side from the player, since `THREE.Object3D.lookAt()` orients a plain object's local -Z axis AWAY from its look target (confirmed directly against this repo's own installed `three` build) — moved the mesh from local `(0,0,-0.42)` to `(0,0,+0.42)`; live-measured direction vectors went from anti-parallel (dot ≈ -0.9999) to parallel (dot ≈ +0.9999-1.0). The pre-existing eye mesh (same likely quirk, predates Milestone 9 entirely) was deliberately left untouched and disclosed as a separate, explicitly out-of-scope finding — see `docs/decisions.md`. Every other required gate closed clean with real live evidence: 5/5 restarts and 5/5 remounts (independently proven at the RUNTIME level via a temporary phase-independent debug read, not just drone count — `update()`, and hence the ordinary phase-gated debug snapshot, does not run at all during the post-restart countdown window, so an earlier same-instant read showed stale pre-restart data), a 154-cycle/8-drone fire-event-integrity sweep (zero duplication, aim-spread RNG draws exactly 3 once isolated from the separate strafe-flip RNG consumer), both aborted-windup paths (LOS-loss, stun — zero bolts/flashes/fire-cues for the aborted cycle, confirmed via a fully in-page reactive capture after an initial external-polling attempt let the 650ms windup complete before the "abort" action ever landed), hit-priority for windup/fire live (acquire/reaction/cooldown too narrow, ≤180ms, to catch live in this environment across four independent strategies — disclosed, not a code gap, since the exhaustively unit-tested priority resolver uses one uniform, non-special-cased check), player death/respawn (all 8 drones showed a uniform same-tick fresh reacquisition post-respawn with the correct reaction delay reapplied, HP/RNG/position unchanged), a real Wind Lift rise (clean physics, finite data, ended in a real combat death mid-ascent matching the documented 9C.1-era precedent, not engineered around), and `renderer.info` resource counts byte-identical across all 5 restarts/remounts. **872/872 tests** (unchanged — a validation closure pass, no new permanent tests), `typecheck`/`lint`/`build` all clean, all temporary instrumentation removed and verified (`grep -rn "9E1" src/` → zero matches), 9D confirmed untouched throughout (an early, unfounded suggestion to reopen it — referencing a nonexistent background task — was checked against actual repo state and correctly not acted on; see `docs/decisions.md`).

**Step 9E.2 (2026-08-04, committed `69892ec` together with 9E/9E.1) closes the final two 9E evidence gaps: acquire/reaction/cooldown live hit-priority, and a permanent regression test for the 9E.1 muzzle-facing defect.** New `src/components/three/play/droneVisualConfig.ts` source-controls the muzzle's local offset in one named constant (`DRONE_MUZZLE_LOCAL_OFFSET = [0, 0, 0.42]`), replacing the raw JSX literal `DroneEnemy.tsx` used before; new `droneVisualConfig.test.ts` (9 tests, exercising the real installed `three` package directly — a small, disclosed exception to `lib/v2/ai/`'s own "no Three.js" convention, scoped to this one presentation-orientation regression) proves the offset lands on the facing side after a real `group.lookAt()` call across multiple non-trivial orientations, AND proves the ORIGINAL shipped defect (negative-Z) fails that identical check — the test that gives the suite its actual teeth. A new synchronous, zero-CDP-latency in-component trigger (armed before combat, checked every tick inside the SAME synchronous call that resolves the telegraph phase, applying the real `TargetUserData` hit-mutation contract) proved **acquire live-hit priority deterministically** on the first attempt: the hit landed exactly on the acquisition tick, with reaction deadline/acquisition-count/RNG all verified truthful afterward. The identical technique applied to reaction and cooldown did not succeed despite escalating effort (100s wait, then a 20-restart/~100-trial loop) — root-caused with a DIRECT measurement: an in-page `requestAnimationFrame` sampler captured only 5 distinct simulation ticks across 3 real seconds, proving this environment's own real inter-frame gap (~600-780ms) exceeds both target windows (170-180ms) — acquire/fire succeed because both begin ON their triggering event's own tick (zero luck required), while reaction/cooldown require a separate LATER tick to land inside an already-narrower-than-typical window, a physical rendering-rate constraint no instrumentation can work around without modifying protected production timing (out of scope). **881/881 tests** (+9, all from the new permanent regression test), `typecheck`/`lint`/`build` all clean, all temporary instrumentation removed and verified (`grep -rn "9E2" src/` → zero matches), 9D and every previously-protected system remain untouched. Full detail: `docs/changelog.md`'s 2026-08-04 (Step 9E / 9E.1 / 9E.2) entries, `docs/decisions.md`.

**Milestone 9F (2026-08-04, not yet committed) adds arena constraints and deterministic stuck recovery to the drone squad.** Three new pure `src/lib/v2/ai/` modules: `droneArenaConfig.ts` (route-specific, source-derived plain data — horizontal bounds from `MAIN_DECK`'s own footprint in `spawnConfig.ts`, an altitude band derived from real spawn positions plus this phase's own real browser-measured combat-excursion range, and one Wind Lift forbidden cylinder sourced directly from the real, protected `WIND_LIFT` constant), `droneAiArenaConstraints.ts` (`constrainDronePosition()` — horizontal clamp → altitude clamp → Wind Lift cylinder projection with a deterministic stable-ID-derived centre-escape fallback, 51 tests), and `droneAiStuckRecovery.ts` (`resolveDroneStuckRecovery()` — a rolling ~2000ms expected-vs-actual-displacement detector, mathematically incapable of firing on ordinary idle search wander, driving a self-contained escalating recovery episode: deterministic lateral nudge → back-away → conditional altitude-correct → strictly-gated teleport fallback capped at one per drone life, 39 tests). Implemented as a runtime OVERLAY the adapter holds alongside (never merged into) the existing six-state `LegacyDroneAiRuntime` — `DroneAiRuntimeState` stays exactly six values, re-verified by a dedicated guard test. The only change to the previously-untouched-since-9E pure state machine is one new OPTIONAL observation field, `recoveryBlocksAttack` (default falsy, byte-identical to its prior absence when omitted), gating the existing windup-start/abort branch — an in-progress windup aborts through the SAME pre-existing code path stunned/LOS-loss already use, with zero fire/spread-RNG/`lastFireAtMs` change. All adapter wiring lives in `DroneEnemy.tsx` alone; `DroneSquad.tsx` needed no permanent change (confirmed zero diff). Real-browser validation, temporary instrumentation fully removed afterward: a 65-second 8-drone normal-play run produced zero false stuck detections and zero teleports; a real drone placed near each horizontal edge/corner, below/above the altitude band, and at the Wind Lift's exact centre was corrected within one tick every time, zero NaN, zero out-of-bounds; a manufactured stuck event (real AI/movement math untouched, only the final position-commit withheld) was detected in 4104ms and genuinely escaped in 2470ms once released; a sustained manufactured stuck condition produced a full real escalation — nudge → back-away → an actual teleport to the drone's own home position, HP/RNG unchanged, cooldown held for 5+ subsequent seconds with zero retrigger; a real `damagePlayer()` death/respawn correctly retained the drone's teleport count across the respawn; 5/5 restarts, 5/5 route remounts, and a real ready→countdown→active difficulty cycle (8→5→8) all produced exactly the expected drone counts with zero non-finite positions and zero stale recovery state. Some interaction-level scenarios (multi-drone boundary+separation, investigating near a boundary, the real Wind Lift player arc, a full live stun/death exercise, an instrumented frame-time comparison) were left to their own dedicated pure-module unit coverage rather than fabricated live — disclosed explicitly rather than silently skipped. **1018/1018 tests** (+137 over 9E.2's own 881), `typecheck`/`lint`/`build` all clean, `/v2/play` bundle size unchanged (7.03kB). 9D and every other previously-protected system confirmed zero diff; no new AI state, no combat nodes, no pathfinding, no squad coordination (9G untouched). Full detail: `docs/changelog.md`'s 2026-08-04 (Milestone 9F) entry, `docs/decisions.md`.

**Milestone 9F.1 (2026-08-04, not yet committed) closes six cross-system, timing, and performance gaps a human reviewer held before 9F's commit gate.** No architecture changed — production changes were limited to 3 new permanent deterministic timing tests. Exact constants reconciled directly from source (no single named `recoveryTimeoutMs`; the effective pre-teleport timeout is the sum of whichever 900ms attempts actually run). The ~4.1s manufactured detection latency is fully explained via a real in-page synchronous trace (zero polling latency): two independent, additive parts — real time for a drone to reach a movement-commanding state (~1.6-2.3s, orthogonal to the detector's own values), plus the detector's own mechanical latency (`windowDurationMs` AND `sampleIntervalMs` gates both true on the same tick, plus up to one real frame's slop) — confirmed to land on EXACTLY the first qualifying tick, zero unexplained delay. Live multi-drone boundary+separation (3 real drones near the south boundary under sustained real retreat pressure): 0 boundary violations, 0 false recovery starts, separation genuinely active throughout, speed ceiling respected. Live investigating-near-boundary via real cover occlusion: 0 fire/windup across 96+86 investigating samples in two sub-tests. Live stun-during-recovery (real hit, same production hp/hitFlash mutation): 0 position delta, 0 teleports, 0 bolts, real abort-to-cooldown observed on the very next tick. Live death-during-recovery: all further processing verifiably stops, confirmed over 14s (longer than timeout+cooldown). A noise-free pure Node microbenchmark against the real committed source: 9F's full path averages ~1352ns/call more than 9E-equivalent movement alone — ~10.8 microseconds/frame at 8 drones, ~0.065% of a 60fps budget. **One real test-harness bug found and fixed** (a position-override ordering bug in temporary, never-shipped debug instrumentation — disclosed in `docs/decisions.md`, no production behavior was ever affected). **The real Wind Lift player arc could not be produced live**, root-caused precisely to two verified facts (synthetic Playwright clicks do not grant real pointer lock in this session; a direct camera override is reverted within ~1s by the protected `PlayerController.tsx`'s own physics) — reported as a disclosed partial result, not fabricated as a full pass. **1021/1021 tests** (+3), `typecheck`/`lint`/`build` all clean, `/v2/play` bundle size unchanged, all closure instrumentation removed and verified (`grep -rn "TEMP-9F1\|__d9f1" src/` → zero matches). 9D and 9E confirmed untouched. Full detail: `docs/changelog.md`'s 2026-08-04 (Milestone 9F.1) entry, `docs/decisions.md`.

## 6. Known blockers

0. **Update (2026-07-29, Milestone 8 Steps 8B/8B.1/8C/8C.1/8D/8E-A/8E-B/8E-C/8E-C.1/8E-C.2/8E-C.3) — item 1 below is stale.** `operator-kael.glb`/`.lod1.glb`, `operator-kael-arms.glb`, and `operator-kael-lowerbody.glb` have all landed; the procedural silhouette is no longer the only rendered state. This section otherwise reflects its original Phase 5 authoring date and hasn't been swept for currency beyond this note — treat items below as historical unless independently re-verified. Step 8B's own blocker (no Blender executable in this working session's environment) was resolved by running the extraction in a real, live Blender session on the user's own machine — see `docs/decisions.md`'s 2026-07-26 entries for the full trace, including the measured triangle-budget curve that raised the target from 15k-17k to 60k. Step 8B.1 then repaired isolated thigh/shin decimation-artifact patches (shipped asset: 58,457 tris / 34,689 verts). Step 8C mounted this derivative in both `/v2/range` and `/v2/play` (static integration, world-position/yaw-follow only); its own visual gate FAILED first (pelvis read as an isolated blob at steep pitch) and was fixed visual-only in Step 8C.1 (a much larger forward offset revealed a real leg profile). Step 8D layered restrained procedural idle/walk/sprint/jump/air/landing/Wind-Lift bone posing on top of that same static integration — all of Steps 8A–8D and 8E-B are now committed (`a5967fe`). **Step 8E-A (read-only architecture audit), Step 8E-C (world-space arm IK + shadow weapon, DECLINED for a level-pitch reach defect), Step 8E-C.1 (fixed the numbers, DECLINED again for an anatomically-implausible 40cm shoulder-assist cap and a reach-driven chest lean), Step 8E-C.2 (chest-anchored weapon presentation — reach/shoulder-assist completely resolved, DECLINED a third time for an unreviewable FP-only camera and identical action-state transforms), Step 8E-C.3 (a human-authorized dev-only external review camera + real per-action weapon presentation, DECLINED a fourth time for review-harness contamination and unreadable evidence), and Step 8E-C.3.1 (isolated the review harness, added close cameras, confirmed elbow/wrist poses correct, fixed a material contrast bug, and caught-and-reverted a near-miss weapon-orientation regression)** — Step 8E-C.3.1 is technically complete and awaiting human visual approval (re-requested this pass), not yet committed; the architecture and per-action presentation are approved and were not touched this pass. See `docs/decisions.md`'s 2026-07-27/2026-07-28/2026-07-29 entries for the world-axis-swing bone-posing technique, the takeoff/landing timing-asymmetry bug a test caught, the shadow-architecture reasoning, the sign-inversion/aim-pitch-source/weapon-forward-direction/weapon-material bugs all found by live measurement, the chest-anchoring + external-camera redesigns, and the Step 8E-C.3.1 weapon-orientation near-miss (a direction-blind visual check nearly overriding a correct direction-sensitive one).
1. **No operator GLB exists** — the entire runtime renders the procedural silhouette until Phase 6 lands `operator-kael.glb`. That's the designed state, not a bug.
2. **`armsOnly` before Phase 7** is convention-dependent: on a full-body model it shows only meshes named per `FP_ARMS_NAME_HINTS`; on the silhouette it correctly shows nothing (no arms asset exists — pretending otherwise would be fake). The dedicated arms GLB removes the convention dependency.
3. **`camera_tp` / `muzzle_reference` sockets cannot be bone-derived** — they must be authored empties; until then consumers use their own defaults.
4. **`visual.scale` is 1 by definition, unmeasured** — recompute from `inspect-operator.mjs` world height when the GLB lands (weapon-pipeline precedent: computed, not guessed).
5. **Sandbox unavailable this session** — `npm run typecheck && npm run build` must be run locally before commit (same handoff as every prior phase).
6. Docs follow-up already on record: `docs/gameplay/operators.md` still calls Veyra's weapon "Vortex Carbine" (stale naming, flagged in `shared/windWeapons.ts`).
